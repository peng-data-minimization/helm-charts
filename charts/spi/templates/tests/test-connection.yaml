apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spi.fullname" . }}-test-connection"
  labels:
    {{- include "spi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
